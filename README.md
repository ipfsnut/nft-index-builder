# NFT URI Index Builder

Generates an index of NFT URIs.

## Setup

```bash
git clone [repository-url]
cd nft-indexer
npm init -y
npm install ethers@5
node getURIs.js
```

The script will output `nft_index.json` containing all URIs, ready for IPFS upload.
